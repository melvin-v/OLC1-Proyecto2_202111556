export enum Type {
    INT,
    BOOLEAN,
    VARCHAR,
    DATE,
    DOUBLE,
    NULL,
    NEGATIVE
}

export enum ArithmeticOperator {
    PLUS,
    MINUS,
    MULT,
    DIV,
    MOD,
    DATE
}

export enum relationalOperator {
    IGUAL       = '==',
    DIF         = '!=',
    MENOR       = '<',
    MENORIGUAL  = '<=',
    MAYOR       = '>',
    MAYORIGUAL  = '>='
}