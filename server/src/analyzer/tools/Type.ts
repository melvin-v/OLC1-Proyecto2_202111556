export enum type {
    INT     = 'int',
    BOOLEAN = 'boolean',
    VARCHAR = 'varchar',
    DATE    = 'date',
    DOUBLE  = 'double',
    NULL    = 'null'
}

export enum arithmeticOperator {
    PLUS    = '+',
    MINUS   = '-',
    MULT    = '*',
    DIV     = '/',
    MOD     = '%'
}

export enum relationalOperator {
    IGUAL       = '==',
    DIF         = '!=',
    MENOR       = '<',
    MENORIGUAL  = '<=',
    MAYOR       = '>',
    MAYORIGUAL  = '>='
}