export enum type {
    INT     = 'int',
    STRING  = 'string',
    BOOLEAN = 'boolean',
    VARCHAR = 'varchar',
    DATE    = 'date',
    DOUBLE  = 'double'
}

export enum arithmeticOperator {
    PLUS    = '+',
    MINUS   = '-',
    MULT    = '*',
    DIV     = '/',
    MOD     = '%'
}

export enum relationalOperator {
    IGUAL       = '==',
    DIF         = '!=',
    MENOR       = '<',
    MENORIGUAL  = '<=',
    MAYOR       = '>',
    MAYORIGUAL  = '>='
}